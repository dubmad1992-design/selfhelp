import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function WorkoutsScreen() {
  return (
    <ScreenPlaceholder title="Workouts" hint="Exercises, sets, and PRs — coming in build step 5." />
  );
}
