import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function DashboardScreen() {
  return (
    <ScreenPlaceholder title="Dashboard" hint="Your day at a glance — coming in build step 3." />
  );
}
