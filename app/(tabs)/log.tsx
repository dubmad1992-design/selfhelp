import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function LogScreen() {
  return (
    <ScreenPlaceholder title="Log" hint="Two taps to log your weight — coming in build step 2." />
  );
}
